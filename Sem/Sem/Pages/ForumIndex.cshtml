@page

@{
    Layout = "Shared/_Layout";
}

<button class="btn-lg btn-success" id="create_forum_item_button" onclick="Redirect('@Url.Page("./Constructor")')">Создать</button>
<div class="container-fluid">
    <div class="row">
        @foreach (var indexForum in Sem.DB_Operations.ChatsOps.GetAllChats())
        {
            @await Component.InvokeAsync("IndexForum", indexForum)
        }
    </div>
</div>